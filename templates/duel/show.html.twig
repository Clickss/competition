{% extends 'base.html.twig' %}

{% block title %}Duel{% endblock %}

{% block body %}
    <h1>Duel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ duel.id }}</td>
            </tr>
            <tr>
                <th>Horaire</th>
                <td>{{ duel.horaire ? duel.horaire|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Score_equipe1</th>
                <td>{{ duel.scoreEquipe1 }}</td>
            </tr>
            <tr>
                <th>Score_equipe2</th>
                <td>{{ duel.scoreEquipe2 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('duel_index') }}">back to list</a>

    <a href="{{ path('duel_edit', {'id': duel.id}) }}">edit</a>

    {{ include('duel/_delete_form.html.twig') }}
{% endblock %}