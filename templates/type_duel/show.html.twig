{% extends 'base.html.twig' %}

{% block title %}TypeMatch{% endblock %}

{% block body %}
    <h1>TypeMatch</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_match.id }}</td>
            </tr>
            <tr>
                <th>Type_match</th>
                <td>{{ type_match.typeMatch }}</td>
            </tr>
            <tr>
                <th>Code_type_match</th>
                <td>{{ type_match.codeTypeMatch }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_match_index') }}">back to list</a>

    <a href="{{ path('type_match_edit', {'id': type_match.id}) }}">edit</a>

    {{ include('type_match/_delete_form.html.twig') }}
{% endblock %}